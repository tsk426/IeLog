<div class="container mt-5">
  <div class="card shadow p-4">
    <h2 class="fw-bold mb-4 text-center">🏷️ タグ管理</h2>

    <%= form_with model: @tag, url: admin_tags_path, local: true do |f| %>
      <div class="input-group mb-3">
        <%= f.text_field :name, class: "form-control", placeholder: "新しいタグ名を入力" %>
        <%= f.submit "追加", class: "btn btn-outline-primary" %>
      </div>
    <% end %>

    <% if @tags.any? %>
      <table class="table table-bordered table-hover">
        <thead class="table-light">
          <tr>
            <th>ID</th>
            <th>タグ名</th>
            <th>操作</th>
          </tr>
        </thead>
        <tbody>
          <% @tags.each do |tag| %>
            <tr>
              <td><%= tag.id %></td>
              <td><%= tag.name %></td>
              <td>
                <%= button_to '削除', admin_tag_path(tag), method: :delete, data: { turbo_confirm: "本当に削除しますか？" }, class: "btn btn-danger btn-sm" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    <% else %>
      <p class="text-muted text-center">登録されているタグはありません。</p>
    <% end %>
  </div>
</div>

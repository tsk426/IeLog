<div class="container mt-5">
  <div class="card shadow">
    <div class="card-body">

      <h2 class="fw-bold mb-4 text-center">🔐 新規会員登録</h2>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="mb-3">
          <%= f.label :name, "ニックネーム", class: "form-label" %>
          <%= f.text_field :name, class: "form-control", placeholder: "例）たけちゃん" %>
        </div>

        <div class="mb-3">
          <%= f.label :email, "メールアドレス", class: "form-label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "例）example@example.com" %>
        </div>

        <div class="mb-3">
          <%= f.label :phone_number, "電話番号", class: "form-label" %>
          <%= f.telephone_field :phone_number, class: "form-control", placeholder: "ハイフンなしで入力（例：09012345678）" %>
        </div>

        <div class="mb-3">
          <%= f.label :password, "パスワード", class: "form-label" %>
          <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "6文字以上" %>
        </div>

        <div class="mb-3">
          <%= f.label :password_confirmation, "パスワード（確認）", class: "form-label" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "もう一度入力してください" %>
        </div>

        <div class="text-center mt-4">
          <%= f.submit "登録する", class: "btn btn-primary btn-lg w-100" %>
        </div>
      <% end %>

    </div>
  </div>
</div>
